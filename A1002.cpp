#incldue <cstdio>
const int maxn = 1000;
int main()
{
	int n;
	scanf("%d", &n);
	int a[maxn], b[maxn];
	float a1[maxn], b1[maxn];
	for(int i = 0; i < n; i++){
		scanf("%d %f", &a[i], &a1[i]);0
	}
	
	int m;
	scanf("%d", &m);
	int c[maxn], d[maxn];
	float c1[maxn], d1[maxn];
	for(int i = 0; i < m; i++){
		scanf("%d %f", &c1[i], &d1[i]);
	}
	int count = 0;
	return 0;	
}